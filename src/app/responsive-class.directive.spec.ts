import { ResponsiveClassDirective } from './responsive-class.directive';

describe('ResponsiveClassDirective', () => {
  it('should create an instance', () => {
    const directive = new ResponsiveClassDirective();
    expect(directive).toBeTruthy();
  });
});
